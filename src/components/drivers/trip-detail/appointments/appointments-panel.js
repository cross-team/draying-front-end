import React from 'react'
import AllAppointments from './all-appointments'

const AppointmentsPanel = ({ draying }) => {
  return <AllAppointments draying={draying} />
}

export default AppointmentsPanel
